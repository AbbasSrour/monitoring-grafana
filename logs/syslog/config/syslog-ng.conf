@version: 4.5
@include "scl.conf"

source s_udp { udp(port(514)); };

destination d_loki {
	syslog("alloy" transport("tcp") port("51893"));
};

log {
        source(s_udp);
        destination(d_loki);
};

